'use strict';

const { body, param, query, validationResult, checkSchema, oneOf } = require('express-validator');

module.exports = {
  body,
  param,
  query,
  validationResult,
  checkSchema,
  oneOf,
};
