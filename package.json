{
  "name": "academix-2.0",
  "version": "2.0.0",
  "description": "Integral academic-administrative platform for New Direction Academy.",
  "private": true,
  "type": "commonjs",
  "directories": {
    "doc": "docs"
  },
  "scripts": {
    "backend:dev": "npm run dev --prefix backend",
    "backend:start": "npm start --prefix backend",
    "backend:test": "npm test --prefix backend",
    "backend:lint": "npm run lint --prefix backend",
    "backend:migrate": "npm run migrate --prefix backend",
    "backend:seed": "npm run seed --prefix backend",
    "frontend:dev": "npm run dev --prefix frontend",
    "frontend:build": "npm run build --prefix frontend",
    "test": "npm test --prefix backend"
  },
  "keywords": []
}
