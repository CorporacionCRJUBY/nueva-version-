# FILE: .env.docker (PLANTILLA lista para Docker)
#
#   cp .env.docker .env
#   bash scripts/init-secrets.sh --demo     # o rellena a mano
#   docker compose up --build -d
#
# ⚠️ NO poner credenciales reales en este archivo: se versiona.
#
# La base de datos es un MySQL NATIVO del anfitrión (no un contenedor).

DB_HOST=host.docker.internal
DB_PORT=3306
DB_USER=ADMIN
DB_PASSWORD=J-admin-crfenix55
DB_NAME=academix_v2
CORS_ORIGIN=http://localhost:8080
TZ=America/Costa_Rica
COOKIE_SECURE=false
SEED_ALLOW_IN_PRODUCTION=false
