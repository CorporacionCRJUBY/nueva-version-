# -----------------------------------------------------------------------------
# frontend/.env.development.example — servidor de desarrollo de Vite.
# Copiar a frontend/.env.development y ajustar.
# -----------------------------------------------------------------------------
# El backend local escucha en el puerto 5000.
VITE_API_URL=http://localhost:5000/api

VITE_APP_NAME=ACADEMIX 2.0
VITE_DEFAULT_LANGUAGE=en
